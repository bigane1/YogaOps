# Nginx : autoriser les uploads d images jusqu a 6 Mo (backoffice YogaOps)
# Ajoutez dans le bloc server {} de votre site, puis :
#   sudo nginx -t && sudo systemctl reload nginx

client_max_body_size 6M;
